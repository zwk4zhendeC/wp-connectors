version: "3.8"

services:
  nginx:
    image: nginx:1.28
    container_name: http-sink-benchmark
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped